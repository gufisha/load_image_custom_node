from setuptools import setup, find_packages

setup(
    name='load_image_custom_folder',
    version='0.1.0',
    description='ComfyUI node to load images from a user-defined folder',
    author='gufisha',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)